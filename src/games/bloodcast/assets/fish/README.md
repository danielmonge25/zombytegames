# Fish art

One image per fish, named after its id in `src/games/bloodcast/data.ts`:
`fish-001.png`, `fish-002.png`, … Transparent PNGs look best.

To reveal a fish's name / rarity / habitat, fill in its fields in `data.ts`
(leave them `null` to keep showing "???").
