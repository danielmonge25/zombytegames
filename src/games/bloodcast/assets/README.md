# BLOODCAST art

Drop `cover.png` (or `.jpg` / `.webp`) here to replace the illustrated night scene
in the BLOODCAST panel on the home page. It's resized and converted to WebP at build time.
A 16:10 image around 1920px wide works best.
